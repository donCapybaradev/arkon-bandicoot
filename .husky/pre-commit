# .husky/pre-commit

# Falla el hook si cualquier comando regresa código != 0
set -e

# 1) Biome (lint + format) sobre archivos staged
bunx lint-staged

# 2) Ejecutar TODOS los tests (modo CI, sin watch)
bun run test:ci